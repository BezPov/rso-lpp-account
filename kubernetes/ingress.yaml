apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: lpp-ingress
spec:
  rules:
    - http:
        paths:
          - path: /account(/|$)(.*)
            backend:
              serviceName: lpp-account-service
              servicePort: 80